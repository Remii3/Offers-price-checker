import { useOffersSection } from "./offersSection.hooks";

export default function OffersSection() {
  const { offers, error, isPending } = useOffersSection();
  return <section>
    
  </section>;
}
